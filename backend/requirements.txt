# Full API/test stack requires Python 3.10+; safe python-multipart releases no longer support 3.9.
# The stdlib core remains Python 3.9-compatible; use requirements-engine-test.txt for its gate.
# --- runtime (API layer) ---
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
python-multipart>=0.0.32  # FastAPI form-data endpoints; importing the app fails without it

# --- persistence (optional store; core runs without it) ---
sqlalchemy>=2.0        # SQLite persistence layer (aurora.store_sql)
alembic>=1.13          # schema migrations for the SQLite store

# --- tests ---
build>=1.2             # build/install wheel portability smoke
setuptools>=68         # no-isolation wheel build backend
wheel>=0.42            # no-isolation wheel build command
pytest>=8.0
hypothesis>=6.100
httpx>=0.27            # FastAPI TestClient

# --- OPTIONAL production accelerators (NOT required at runtime) ---
# The core discovery engine is pure standard-library Python for determinism and
# offline operation. These are optional swap-ins documented in the ADRs and are
# intentionally left commented so the base install stays light and works on
# restricted networks.
# scikit-learn>=1.4    # alternative clustering / TF-IDF
# networkx>=3.2        # alternative graph community detection
# scipy>=1.12
# polars>=0.20
