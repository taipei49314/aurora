# Source-checkout and Docker compatibility launcher. The distributable config
# and sole migration chain live inside the aurora package.
[alembic]
script_location = %(here)s/aurora/migrations
prepend_sys_path = %(here)s
sqlalchemy.url = sqlite:///aurora.db
