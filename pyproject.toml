[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "aurora-discovery"
version = "0.1.49"
description = "Local-first unknown-industry discovery engine (deterministic, no LLM at runtime)"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.9"
authors = [{ name = "taipei49314" }]
keywords = [
  "research",
  "discovery",
  "reproducible",
  "local-first",
  "deterministic",
  "epistemic",
]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Intended Audience :: Science/Research",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.9",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Topic :: Scientific/Engineering :: Information Analysis",
]
# Core engine is stdlib-only; optional extras install API/test deps.
dependencies = []

[project.optional-dependencies]
persistence = [
  "sqlalchemy>=2.0",
  "alembic>=1.13",
]
api = [
  "fastapi>=0.110",
  "uvicorn[standard]>=0.29",
  "pydantic>=2.6",
  "python-multipart>=0.0.32",
  "sqlalchemy>=2.0",
  "alembic>=1.13",
]
test = [
  "build>=1.2",
  "setuptools>=68",
  "wheel>=0.42",
  "pytest>=8.0",
  "hypothesis>=6.100",
  "httpx>=0.27",
  "fastapi>=0.110",
  "pydantic>=2.6",
  "python-multipart>=0.0.32",
  "sqlalchemy>=2.0",
  "alembic>=1.13",
]
dev = [
  "aurora-discovery[api,test]",
]

[project.urls]
Homepage = "https://github.com/taipei49314/aurora"
Repository = "https://github.com/taipei49314/aurora"
Issues = "https://github.com/taipei49314/aurora/issues"
Changelog = "https://github.com/taipei49314/aurora/blob/master/CHANGELOG.md"

[tool.setuptools.packages.find]
where = ["backend"]
include = ["aurora*"]

[tool.setuptools.package-data]
aurora = [
  "alembic.ini",
  "migrations/README",
  "migrations/env.py",
  "migrations/script.py.mako",
  "migrations/versions/*.py",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["backend", "."]
